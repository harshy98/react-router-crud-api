# react-router-crud-api

# Boilerplate code for simple CRUD operations using fetch API in JavaScript. Also has feature for routing to different links.

# Installation: Clone the repo and run 'npm install'
